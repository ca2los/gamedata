    // 00. TYPE OF MEDIA
    
    class Media {
        constructor(name) {
            this.name = name;
        }
        get_role() {
            return "Type of Media";
        }
    }

    module.exports = Media;

    // The 'VideoGame' class holding the 'constructor' operator is setting the parameters to start building specific roles.
    // The 'team' function prints the data into the console to check if the data has been returned.